<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body{
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        form{
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            margin-top: 25px;
        }
        input{
            margin: 10px;
            padding: 10px;
            width: 300px;
        }
        button{
            padding: 10px;
            width: 300px;
            background-color: #4CAF50;
            color: white;
            border: none;
            cursor: pointer;
        }
        button:hover{
            background-color: #45a049;
        }
        table{
            margin-top: 30px;
            margin-left: auto;
            margin-right: auto;
        }
        table, th, td {
            border: 1px solid black;
            border-collapse: collapse;
        }
        th, td, tr{
            padding: 15px;
        }
        th , tr, td{
            text-align: center;
        }   
        .logout{
            padding: 10px;
            background-color: #4CAF50;
            color: white;
            border: none;
            cursor: pointer;
            text-align: center;
            text-decoration: none;
            margin-top: 30px;
        }
        .logout:hover{
            background-color: #45a049;
        }

    </style>
    <title>link shortner</title>
</head>
<body>

    <h1>link shortner</h1>
    <!-- svds -->

    <h2>user name : <%= user.name  %></h2>
    <form action="" method="post">
        <label for="name">website name</label>
        <input type="text" name="name">
        <br>
        <label for="link">link</label>
        <input type="text" name="URL" id="url" placeholder="https://example.com/">
        <br>
        <button type="submit">submit</button>
    </form>

        <table>
            <% if (entries.length != 0) { %>
             
                <tr>
                    <th>website name</th>
                    <th>short link</th>
                    <th>clicks</th>
                </tr>
                <% } %>
            <% for(let i=0; i<entries.length; i++){ %>
                <tr>
                    <td><%= entries[i].name %></td>
                    <td><a href="https://url-shortner-nine-wheat.vercel.app/<%=entries[i].sURL%>"><%=entries[i].sURL%></a></td>
                    <td><%= entries[i].clicks %></td>
                </tr>
            <% } %>
        </table>

        <a href="/logout" class="logout">Logout</a>
        
</body>
</html>
